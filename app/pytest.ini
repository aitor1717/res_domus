[pytest]
# tests/e2e/ needs Playwright + a Chromium download (`playwright install chromium`)
# and spins up a real Flask subprocess, so it's excluded from the default
# `pytest tests/` run — run it explicitly with `pytest tests/e2e -v`.
addopts = --ignore=tests/e2e
